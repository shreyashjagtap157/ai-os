[Unit]
Description=AI-OS Input Service
After=aios-agent.service
Requires=aios-agent.service

[Service]
Type=simple
ExecStart=/usr/sbin/aios-input
Restart=always
RestartSec=3

# Input device access
SupplementaryGroups=input
PrivateTmp=true

[Install]
WantedBy=multi-user.target
